closure: a closure is a feature in java script where an inner function has access to the outer(enclosing
)

hooks:
hooks were a new addition in react 16.8
 they let you use state and other react featurea without writing a class
hooks=15
setState =updater bcz it used for updating the state to a different value and trigger now

lifting state up concept

components types
1.logical/smart/have state varibles
2.presentation/dump only show ui/they do not  have state variable/props

//function as probs
//js functions are 1st class objects
//this means they can be passes to a function as argument,returned from it & assigned to a variable.


//useFormik
for validatin we can use formik .

UseEffect()
thr hook lets you perform side effect in function components

data fetching, stting up a subscription and manually changing the dom in react components are all examples of side effects.